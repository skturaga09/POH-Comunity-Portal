{
  "name": "poh-community-portal",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "deploy": "node scripts/deploy.mjs",
    "deploy:staging": "firebase hosting:channel:deploy staging --project poh-community-portal",
    "deploy:prod": "node scripts/deploy.mjs hosting --project=poh-community-portal",
    "deploy:functions": "node scripts/deploy.mjs functions --project=poh-community-portal"
  },
  "dependencies": {
    "qrcode-generator": "^1.4.4"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
